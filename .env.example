# Frontend build configuration
# Point to your deployed backend root URL (no trailing /api)
VITE_BACKEND_URL=http://localhost:3001

# Optional: if frontend makes direct calls
VITE_GROQ_API_KEY=your-groq-api-key
VITE_MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=Cluster0

# (Google client ID now comes from backend; no VITE_GOOGLE_CLIENT_ID needed on frontend)
